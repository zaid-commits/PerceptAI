import { Ace } from "../test";

const AIChat: React.FC = () => {
    return (
        <div className="ai-chat-container h-screen bg-black text-white">
            <Ace />
        </div>
    );
};

export default AIChat;
